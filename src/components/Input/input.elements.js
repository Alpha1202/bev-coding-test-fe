import { styled } from "@mui/material/styles";
import { AppBar, TextField, FormControl, Box, OutlinedInput, IconButton, InputAdornment, InputLabel, Toolbar, Typography, Menu, Container, Avatar, Button, Tooltip, MenuItem } from "@mui/material";
import MenuIcon from "@mui/icons-material/Menu";
import CloseIcon from '@mui/icons-material/Close';
import ArrowBackIosNewIcon from '@mui/icons-material/ArrowBackIosNew';

import { COLORS } from "../../constants/theme";

export const TextInput = styled(TextField)`

 
`;

export const Formcontrol = styled(FormControl)`
  width: 100%;
  border-radius: 25px;
`;
export const Inputlabel = styled(InputLabel)`

`;

export const Outlinedinput = styled(OutlinedInput)`

`;

export const Inputadornment = styled(InputAdornment)``;