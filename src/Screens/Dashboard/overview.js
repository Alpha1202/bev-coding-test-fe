import { SectionWrapper, SectionTitle, SectionText } from "./dashboard.elements";
const Overview = () => {
	return (
		<SectionWrapper>
			<SectionTitle variant="h6">
				Overview
				<SectionText>This is dashboard overview. Here, the implementation for the dashboard overview lives</SectionText>
			</SectionTitle>
		</SectionWrapper>
	);
};

export default Overview;
