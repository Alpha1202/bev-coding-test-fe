import { SectionWrapper, SectionTitle, SectionText } from "./dashboard.elements";

const History = () => {
	return (
		<SectionWrapper>
			<SectionTitle variant="h6">
				History
				<SectionText>This is dashboard History. Here, the implementation for the dashboard History lives</SectionText>
			</SectionTitle>
		</SectionWrapper>
	);
};

export default History;
